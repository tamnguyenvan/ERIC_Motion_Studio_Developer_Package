# Resources

Immutable runtime assets are packaged under
`src/eric_motion_studio/resources/`. This top-level boundary is reserved for
future source assets and build-time transformations; mutable user data must
never be stored here.
