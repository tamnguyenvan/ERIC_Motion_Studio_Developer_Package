# Simple Motion Library workflow backup

`pre-change-source-cd840e9.tar.gz` contains the baseline versions of every
tracked file changed when the draft/approval workflow was replaced with the
Duplicate, Save, and Delete workflow.

Baseline commit:

```text
cd840e9c885b033e2e646ef80e4bc9c357732076
```

List the archive without extracting:

```bash
tar -tzf backups/simple-library-workflow/pre-change-source-cd840e9.tar.gz
```
