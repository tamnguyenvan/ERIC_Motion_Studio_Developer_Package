# Gesture command framework backup

`pre-change-source-8a7a06b.tar.gz` contains the baseline versions of the
existing files modified for the deterministic semantic command update.

Baseline commit:

```text
8a7a06bae077d80e80b1ea16b6a2d7ba87af75ba
```

Inspect without replacing working files:

```bash
tar -tzf backups/gesture-command-framework/pre-change-source-8a7a06b.tar.gz
```

Extract into a separate directory:

```bash
mkdir -p /tmp/eric-motion-studio-pre-change
tar -xzf backups/gesture-command-framework/pre-change-source-8a7a06b.tar.gz \
  -C /tmp/eric-motion-studio-pre-change
```
