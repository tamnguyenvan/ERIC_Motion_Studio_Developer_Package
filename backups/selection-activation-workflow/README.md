# Selection activation workflow backup

`pre-change-source-3bef5b4.tar.gz` contains the baseline versions of every
tracked file changed when library selection became the single motion activation
path.

Baseline commit:

```text
3bef5b4ea82da6f1337e5a4bd2bc250a775e268a
```

List the archive without extracting:

```bash
tar -tzf backups/selection-activation-workflow/pre-change-source-3bef5b4.tar.gz
```
