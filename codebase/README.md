# Read-Only Legacy Backup

`codebase/` preserves the pre-refactor ERIC Gesture Lab implementation and its
historical regression fixtures. It is not a supported application source tree.

Do not launch, import, edit, or write runtime data here. The supported application
is the `eric_motion_studio` package under `src/`; use the entry points documented
in `README_FOR_DEVELOPER.txt`.

Legacy tests may read this backup only to verify behavioral parity. Cleanup and
archival decisions remain deferred to Milestone 7.
